import QtQuick
import QtQuick.Controls as Controls
import org.kde.kirigami as Kirigami

Kirigami.Page {
    title: i18n("Export")

    Controls.Label {
        anchors.centerIn: parent
        text: i18n("Export page placeholder.")
    }
}
